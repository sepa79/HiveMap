# Simple Bootstrap Prompt

Use this if the AI tool is weak or easily confused.

1. Read `AGENTS.md`, `docs/README.md`, and `README.md`.
2. Find build files, test files, CI files, and the relevant specs.
3. Identify the tech stack and the local runtime shape.
4. Do not weaken existing SSOT or repo rules just to match a starter template.
5. Update placeholder files in `docs/ai/` and `docs/ARCHITECTURE.md`.
6. Treat `docs/architecture.md` as the canonical architecture source.
7. Add `TODO:` only for real unknowns.
8. Do not change application code.
9. Stop and summarize.

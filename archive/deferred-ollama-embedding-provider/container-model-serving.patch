diff --git b/Dockerfile a/Dockerfile
index 1e56514..329d401 100644
--- b/Dockerfile
+++ a/Dockerfile
@@ -20,15 +20,21 @@ FROM pgvector/pgvector:pg16-bookworm AS runtime
 ENV NODE_ENV=production \
   HIVEMAP_API_HOST=0.0.0.0 \
   HIVEMAP_API_PORT=8787 \
+  HIVEMAP_STATE_DIR=/var/lib/hivemap \
   HIVEMAP_POSTGRES_DB=hivemap \
   HIVEMAP_POSTGRES_PORT=5432 \
   HIVEMAP_DATA_DIR=/var/lib/hivemap/postgres \
+  HIVEMAP_OLLAMA_ENABLED=0 \
+  HIVEMAP_OLLAMA_HOST=127.0.0.1:11434 \
+  HIVEMAP_OLLAMA_MODELS_DIR=/var/lib/hivemap/ollama \
+  HIVEMAP_OLLAMA_PULL_MODELS= \
   HIVEMAP_WEB_DIST=/app/apps/web/dist
 
 WORKDIR /app
 
 RUN apt-get update \
-  && apt-get install -y --no-install-recommends git \
+  && apt-get install -y --no-install-recommends ca-certificates curl git zstd \
+  && curl -fsSL https://ollama.com/download/ollama-linux-amd64.tar.zst | tar --zstd -x -C /usr \
   && rm -rf /var/lib/apt/lists/*
 
 COPY --from=node:22-bookworm-slim /usr/local /usr/local
@@ -39,11 +45,11 @@ COPY --from=build /app/packages ./packages
 COPY docker/hivemap-entrypoint.sh /usr/local/bin/hivemap-entrypoint
 
 RUN chmod +x /usr/local/bin/hivemap-entrypoint \
-  && mkdir -p /var/lib/hivemap/postgres \
+  && mkdir -p /var/lib/hivemap/postgres /var/lib/hivemap/ollama \
   && chown -R postgres:postgres /var/lib/hivemap
 
 EXPOSE 8787
-VOLUME ["/var/lib/hivemap/postgres"]
+VOLUME ["/var/lib/hivemap"]
 
 HEALTHCHECK --interval=10s --timeout=5s --start-period=20s --retries=5 CMD node --input-type=module -e "const response = await fetch('http://127.0.0.1:8787/workspaces'); if (!response.ok) process.exit(1);"
 
diff --git b/deploy/hiveforge/components/stack/ansible/deploy.yml a/deploy/hiveforge/components/stack/ansible/deploy.yml
index f86a13c..4f1c68d 100644
--- b/deploy/hiveforge/components/stack/ansible/deploy.yml
+++ a/deploy/hiveforge/components/stack/ansible/deploy.yml
@@ -8,10 +8,13 @@
     hivemap_profile: "{{ lookup('ansible.builtin.env', 'HIVEFORGE_PROFILE') }}"
     hivemap_compose_template: "{{ playbook_dir }}/templates/docker-compose.yml.j2"
     hivemap_compose_file: "{{ hiveforge_root }}/stacks/compose.yml"
-    hivemap_state_root: "{{ hiveforge_root }}/state/postgres"
+    hivemap_state_root: "{{ hiveforge_root }}/state/hivemap"
     hivemap_data_bind_source: "{{ lookup('ansible.builtin.env', 'HIVEMAP_DATA_BIND_SOURCE') }}"
     hivemap_swarm_placement_constraint: "{{ lookup('ansible.builtin.env', 'HIVEMAP_SWARM_PLACEMENT_CONSTRAINT') }}"
     hivemap_image: "{{ lookup('ansible.builtin.env', 'HIVEMAP_IMAGE') }}"
+    hivemap_ollama_enabled: "{{ lookup('ansible.builtin.env', 'HIVEMAP_OLLAMA_ENABLED') | default('0', true) }}"
+    hivemap_ollama_base_url: "{{ lookup('ansible.builtin.env', 'HIVEMAP_OLLAMA_BASE_URL') }}"
+    hivemap_ollama_pull_models: "{{ lookup('ansible.builtin.env', 'HIVEMAP_OLLAMA_PULL_MODELS') }}"
     hivemap_http_port: "{{ lookup('ansible.builtin.env', 'HIVEMAP_HTTP_PORT') | default('8787', true) }}"
     hivemap_postgres_db: "{{ lookup('ansible.builtin.env', 'HIVEMAP_POSTGRES_DB') | default('hivemap', true) }}"
     hivemap_postgres_port: "{{ lookup('ansible.builtin.env', 'HIVEMAP_POSTGRES_PORT') | default('5432', true) }}"
diff --git b/deploy/hiveforge/components/stack/ansible/tasks/render-compose.yml a/deploy/hiveforge/components/stack/ansible/tasks/render-compose.yml
index e9a1ff2..7d04f2b 100644
--- b/deploy/hiveforge/components/stack/ansible/tasks/render-compose.yml
+++ a/deploy/hiveforge/components/stack/ansible/tasks/render-compose.yml
@@ -12,7 +12,7 @@
 
 - name: Ensure HiveMap state directory exists
   ansible.builtin.file:
-    path: "{{ hivemap_state_root }}/data"
+    path: "{{ hivemap_state_root }}"
     state: directory
     mode: "0755"
 
diff --git b/deploy/hiveforge/components/stack/ansible/tasks/validate-profile.yml a/deploy/hiveforge/components/stack/ansible/tasks/validate-profile.yml
index 9034efe..cea298f 100644
--- b/deploy/hiveforge/components/stack/ansible/tasks/validate-profile.yml
+++ a/deploy/hiveforge/components/stack/ansible/tasks/validate-profile.yml
@@ -45,3 +45,15 @@
     that:
       - hivemap_postgres_port | length > 0
     fail_msg: "HIVEMAP_POSTGRES_PORT must resolve to a non-empty value."
+
+- name: Require supported HiveMap Ollama bundling mode
+  ansible.builtin.assert:
+    that:
+      - hivemap_ollama_enabled in ['0', '1']
+    fail_msg: "HIVEMAP_OLLAMA_ENABLED must be 0 or 1."
+
+- name: Prevent external Ollama URL when bundled Ollama is enabled
+  ansible.builtin.assert:
+    that:
+      - hivemap_ollama_enabled != '1' or hivemap_ollama_base_url | length == 0
+    fail_msg: "Unset HIVEMAP_OLLAMA_BASE_URL when HIVEMAP_OLLAMA_ENABLED=1 so the container uses its bundled local Ollama."
diff --git b/deploy/hiveforge/components/stack/ansible/templates/docker-compose.yml.j2 a/deploy/hiveforge/components/stack/ansible/templates/docker-compose.yml.j2
index c3ee4d6..3aaff0b 100644
--- b/deploy/hiveforge/components/stack/ansible/templates/docker-compose.yml.j2
+++ a/deploy/hiveforge/components/stack/ansible/templates/docker-compose.yml.j2
@@ -5,14 +5,22 @@ services:
     environment:
       HIVEMAP_API_HOST: "0.0.0.0"
       HIVEMAP_API_PORT: "8787"
+      HIVEMAP_STATE_DIR: "/var/lib/hivemap"
       HIVEMAP_POSTGRES_DB: "{{ hivemap_postgres_db }}"
       HIVEMAP_POSTGRES_PORT: "{{ hivemap_postgres_port }}"
       HIVEMAP_DATA_DIR: "/var/lib/hivemap/postgres"
+      HIVEMAP_OLLAMA_ENABLED: "{{ hivemap_ollama_enabled }}"
+      HIVEMAP_OLLAMA_HOST: "127.0.0.1:11434"
+      HIVEMAP_OLLAMA_MODELS_DIR: "/var/lib/hivemap/ollama"
+      HIVEMAP_OLLAMA_PULL_MODELS: "{{ hivemap_ollama_pull_models }}"
       HIVEMAP_WEB_DIST: "/app/apps/web/dist"
+{% if hivemap_ollama_enabled != '1' and hivemap_ollama_base_url | length > 0 %}
+      HIVEMAP_OLLAMA_BASE_URL: "{{ hivemap_ollama_base_url }}"
+{% endif %}
     ports:
       - "{{ hivemap_http_port }}:8787"
     volumes:
-      - "{{ (hivemap_profile == 'docker-swarm') | ternary(hivemap_data_bind_source, hivemap_state_root ~ '/data') }}:/var/lib/hivemap/postgres"
+      - "{{ (hivemap_profile == 'docker-swarm') | ternary(hivemap_data_bind_source, hivemap_state_root) }}:/var/lib/hivemap"
     deploy:
       replicas: 1
       restart_policy:
diff --git b/deploy/hiveforge/components/stack/ansible/update.yml a/deploy/hiveforge/components/stack/ansible/update.yml
index 78c5d44..75b879b 100644
--- b/deploy/hiveforge/components/stack/ansible/update.yml
+++ a/deploy/hiveforge/components/stack/ansible/update.yml
@@ -8,10 +8,13 @@
     hivemap_profile: "{{ lookup('ansible.builtin.env', 'HIVEFORGE_PROFILE') }}"
     hivemap_compose_template: "{{ playbook_dir }}/templates/docker-compose.yml.j2"
     hivemap_compose_file: "{{ hiveforge_root }}/stacks/compose.yml"
-    hivemap_state_root: "{{ hiveforge_root }}/state/postgres"
+    hivemap_state_root: "{{ hiveforge_root }}/state/hivemap"
     hivemap_data_bind_source: "{{ lookup('ansible.builtin.env', 'HIVEMAP_DATA_BIND_SOURCE') }}"
     hivemap_swarm_placement_constraint: "{{ lookup('ansible.builtin.env', 'HIVEMAP_SWARM_PLACEMENT_CONSTRAINT') }}"
     hivemap_image: "{{ lookup('ansible.builtin.env', 'HIVEMAP_IMAGE') }}"
+    hivemap_ollama_enabled: "{{ lookup('ansible.builtin.env', 'HIVEMAP_OLLAMA_ENABLED') | default('0', true) }}"
+    hivemap_ollama_base_url: "{{ lookup('ansible.builtin.env', 'HIVEMAP_OLLAMA_BASE_URL') }}"
+    hivemap_ollama_pull_models: "{{ lookup('ansible.builtin.env', 'HIVEMAP_OLLAMA_PULL_MODELS') }}"
     hivemap_http_port: "{{ lookup('ansible.builtin.env', 'HIVEMAP_HTTP_PORT') | default('8787', true) }}"
     hivemap_postgres_db: "{{ lookup('ansible.builtin.env', 'HIVEMAP_POSTGRES_DB') | default('hivemap', true) }}"
     hivemap_postgres_port: "{{ lookup('ansible.builtin.env', 'HIVEMAP_POSTGRES_PORT') | default('5432', true) }}"
diff --git b/deploy/hiveforge/components/stack/ansible/upgrade.yml a/deploy/hiveforge/components/stack/ansible/upgrade.yml
index 210f001..362e414 100644
--- b/deploy/hiveforge/components/stack/ansible/upgrade.yml
+++ a/deploy/hiveforge/components/stack/ansible/upgrade.yml
@@ -8,10 +8,13 @@
     hivemap_profile: "{{ lookup('ansible.builtin.env', 'HIVEFORGE_PROFILE') }}"
     hivemap_compose_template: "{{ playbook_dir }}/templates/docker-compose.yml.j2"
     hivemap_compose_file: "{{ hiveforge_root }}/stacks/compose.yml"
-    hivemap_state_root: "{{ hiveforge_root }}/state/postgres"
+    hivemap_state_root: "{{ hiveforge_root }}/state/hivemap"
     hivemap_data_bind_source: "{{ lookup('ansible.builtin.env', 'HIVEMAP_DATA_BIND_SOURCE') }}"
     hivemap_swarm_placement_constraint: "{{ lookup('ansible.builtin.env', 'HIVEMAP_SWARM_PLACEMENT_CONSTRAINT') }}"
     hivemap_image: "{{ lookup('ansible.builtin.env', 'HIVEMAP_IMAGE') }}"
+    hivemap_ollama_enabled: "{{ lookup('ansible.builtin.env', 'HIVEMAP_OLLAMA_ENABLED') | default('0', true) }}"
+    hivemap_ollama_base_url: "{{ lookup('ansible.builtin.env', 'HIVEMAP_OLLAMA_BASE_URL') }}"
+    hivemap_ollama_pull_models: "{{ lookup('ansible.builtin.env', 'HIVEMAP_OLLAMA_PULL_MODELS') }}"
     hivemap_http_port: "{{ lookup('ansible.builtin.env', 'HIVEMAP_HTTP_PORT') | default('8787', true) }}"
     hivemap_postgres_db: "{{ lookup('ansible.builtin.env', 'HIVEMAP_POSTGRES_DB') | default('hivemap', true) }}"
     hivemap_postgres_port: "{{ lookup('ansible.builtin.env', 'HIVEMAP_POSTGRES_PORT') | default('5432', true) }}"
diff --git b/docker-compose.yml a/docker-compose.yml
index 243f12c..06b141d 100644
--- b/docker-compose.yml
+++ a/docker-compose.yml
@@ -8,9 +8,14 @@ services:
     environment:
       HIVEMAP_API_HOST: "0.0.0.0"
       HIVEMAP_API_PORT: "8787"
+      HIVEMAP_STATE_DIR: "/var/lib/hivemap"
       HIVEMAP_POSTGRES_DB: "hivemap"
       HIVEMAP_POSTGRES_PORT: "5432"
       HIVEMAP_DATA_DIR: "/var/lib/hivemap/postgres"
+      HIVEMAP_OLLAMA_ENABLED: "${HIVEMAP_OLLAMA_ENABLED:-0}"
+      HIVEMAP_OLLAMA_HOST: "127.0.0.1:11434"
+      HIVEMAP_OLLAMA_MODELS_DIR: "/var/lib/hivemap/ollama"
+      HIVEMAP_OLLAMA_PULL_MODELS: "${HIVEMAP_OLLAMA_PULL_MODELS:-}"
       HIVEMAP_WEB_DIST: "/app/apps/web/dist"
     volumes:
-      - ./.local/hivemap-postgres:/var/lib/hivemap/postgres
+      - ./.local/hivemap-state:/var/lib/hivemap
diff --git b/docker/hivemap-entrypoint.sh a/docker/hivemap-entrypoint.sh
index c1e94a9..6684abd 100644
--- b/docker/hivemap-entrypoint.sh
+++ a/docker/hivemap-entrypoint.sh
@@ -1,31 +1,45 @@
 #!/usr/bin/env bash
 set -euo pipefail
 
-DATA_DIR="${HIVEMAP_DATA_DIR:-/var/lib/hivemap/postgres}"
+STATE_DIR="${HIVEMAP_STATE_DIR:-/var/lib/hivemap}"
+DATA_DIR="${HIVEMAP_DATA_DIR:-${STATE_DIR}/postgres}"
 POSTGRES_DB="${HIVEMAP_POSTGRES_DB:-hivemap}"
 POSTGRES_PORT="${HIVEMAP_POSTGRES_PORT:-5432}"
 API_HOST="${HIVEMAP_API_HOST:-0.0.0.0}"
 API_PORT="${HIVEMAP_API_PORT:-8787}"
+OLLAMA_ENABLED="${HIVEMAP_OLLAMA_ENABLED:-0}"
+OLLAMA_HOST="${HIVEMAP_OLLAMA_HOST:-127.0.0.1:11434}"
+OLLAMA_MODELS_DIR="${HIVEMAP_OLLAMA_MODELS_DIR:-${STATE_DIR}/ollama}"
+OLLAMA_PULL_MODELS="${HIVEMAP_OLLAMA_PULL_MODELS:-}"
 WEB_DIST="${HIVEMAP_WEB_DIST:-/app/apps/web/dist}"
+OLLAMA_PID=""
 
 if [[ ! "${POSTGRES_DB}" =~ ^[A-Za-z_][A-Za-z0-9_]*$ ]]; then
   echo "HIVEMAP_POSTGRES_DB must match ^[A-Za-z_][A-Za-z0-9_]*$" >&2
   exit 1
 fi
 
+if [[ "${OLLAMA_ENABLED}" != "0" && "${OLLAMA_ENABLED}" != "1" ]]; then
+  echo "HIVEMAP_OLLAMA_ENABLED must be 0 or 1" >&2
+  exit 1
+fi
+
 if [[ "$(id -u)" == "0" ]]; then
-  mkdir -p "${DATA_DIR}"
-  chown -R postgres:postgres "${DATA_DIR}"
+  mkdir -p "${DATA_DIR}" "${OLLAMA_MODELS_DIR}"
+  chown -R postgres:postgres "${STATE_DIR}"
   exec gosu postgres "$0" "$@"
 fi
 
-mkdir -p "${DATA_DIR}"
+mkdir -p "${DATA_DIR}" "${OLLAMA_MODELS_DIR}"
 
 if [[ ! -s "${DATA_DIR}/PG_VERSION" ]]; then
   initdb --auth=trust --username=postgres -D "${DATA_DIR}" >/dev/null
 fi
 
 cleanup() {
+  if [[ -n "${OLLAMA_PID}" ]]; then
+    kill "${OLLAMA_PID}" >/dev/null 2>&1 || true
+  fi
   pg_ctl -D "${DATA_DIR}" -m fast stop >/dev/null 2>&1 || true
 }
 
@@ -43,6 +57,48 @@ if [[ "$(psql --host 127.0.0.1 --port "${POSTGRES_PORT}" --username postgres --d
   createdb --host 127.0.0.1 --port "${POSTGRES_PORT}" --username postgres "${POSTGRES_DB}"
 fi
 
+if [[ "${OLLAMA_ENABLED}" == "1" ]]; then
+  LOCAL_OLLAMA_BASE_URL="http://${OLLAMA_HOST}"
+  if [[ -n "${HIVEMAP_OLLAMA_BASE_URL:-}" && "${HIVEMAP_OLLAMA_BASE_URL}" != "${LOCAL_OLLAMA_BASE_URL}" ]]; then
+    echo "HIVEMAP_OLLAMA_BASE_URL must be ${LOCAL_OLLAMA_BASE_URL} when HIVEMAP_OLLAMA_ENABLED=1" >&2
+    exit 1
+  fi
+
+  export OLLAMA_HOST
+  export OLLAMA_MODELS="${OLLAMA_MODELS_DIR}"
+  export HIVEMAP_OLLAMA_BASE_URL="${LOCAL_OLLAMA_BASE_URL}"
+
+  ollama serve >/tmp/hivemap-ollama.log 2>&1 &
+  OLLAMA_PID="$!"
+
+  for _ in $(seq 1 60); do
+    if curl -fsS "${LOCAL_OLLAMA_BASE_URL}/api/tags" >/dev/null 2>&1; then
+      break
+    fi
+    sleep 1
+  done
+
+  if ! curl -fsS "${LOCAL_OLLAMA_BASE_URL}/api/tags" >/dev/null 2>&1; then
+    echo "Bundled Ollama did not become ready at ${LOCAL_OLLAMA_BASE_URL}" >&2
+    exit 1
+  fi
+
+  if [[ -n "${OLLAMA_PULL_MODELS}" ]]; then
+    OLD_IFS="${IFS}"
+    IFS=','
+    read -r -a OLLAMA_MODELS_TO_PULL <<< "${OLLAMA_PULL_MODELS}"
+    IFS="${OLD_IFS}"
+    for MODEL in "${OLLAMA_MODELS_TO_PULL[@]}"; do
+      TRIMMED_MODEL="$(echo "${MODEL}" | xargs)"
+      if [[ -z "${TRIMMED_MODEL}" ]]; then
+        echo "HIVEMAP_OLLAMA_PULL_MODELS must not contain empty model entries" >&2
+        exit 1
+      fi
+      ollama pull "${TRIMMED_MODEL}"
+    done
+  fi
+fi
+
 exec node /app/apps/api/dist/server.js \
   --host "${API_HOST}" \
   --port "${API_PORT}" \
